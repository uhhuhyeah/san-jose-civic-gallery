{
  "private": true,
  "type": "module",
  "description": "Simbli browser fetch primitive driven by Simbli::Client. Browsers install to PLAYWRIGHT_BROWSERS_PATH (set in the Dockerfile).",
  "dependencies": {
    "playwright": "1.60.0"
  }
}
